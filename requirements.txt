# Evrensel Novel -> TXT -> MP3 -> EPUB -> Çeviri     kurulum:  pip install -r requirements.txt
curl_cffi>=0.7.0        # tarayıcı TLS parmak izi taklidi (Cloudflare'a takılmadan hızlı HTTP)
trafilatura>=1.12.0     # evrensel ana metin çıkarma (site CSS'ine bağımlı değil)
beautifulsoup4>=4.12    # yedek metin çıkarma + "sonraki bölüm" linki tespiti
lxml>=5.0               # hızlı HTML ayrıştırıcı
edge-tts>=6.1.0         # Microsoft Edge sesleriyle seslendirme
tqdm>=4.66              # ilerleme çubuğu
playwright>=1.45        # JavaScript ile yüklenen / bot korumalı siteler için gerçek tarayıcı

# --- isteğe bağlı: yalnızca ilgili komutu kullanacaksanız kurun (script zaten otomatik kurmayı dener) ---
ebooklib>=0.18           # `epub` komutu / --format epub  (e-kitap üretimi)
deep-translator>=1.11.4  # `translate` komutu, --translate-engine google (varsayılan; ücretsiz, key yok, internet ister)
# argostranslate>=1.9.6  # --translate-engine argos       (tamamen çevrimdışı; ilk seferde ~200-300 MB model indirir)
pytesseract>=0.3.10      # `ocr` komutu (webtoon/görsel tabanlı bölümler) — pip paketi yalnızca Python sarmalayıcı,
Pillow>=10.0              # sistemde ayrıca tesseract-ocr binary'si kurulu olmalı:
                          #   Ubuntu/Debian: sudo apt install tesseract-ocr tesseract-ocr-tur tesseract-ocr-eng
                          #   macOS:         brew install tesseract tesseract-lang

# Playwright kullanacaksanız bir kez tarayıcıyı da indirin:
#   python -m playwright install chromium          (Linux sunucuda: ... install --with-deps chromium)
